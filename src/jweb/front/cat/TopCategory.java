package jweb.front.cat;

// Essentially a "node," in the W3C's language.
// This includes text nodes as well as elements.

public interface TopCategory {
    @Override
    public String toString();
}
